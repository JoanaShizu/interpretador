#include <stdio.h>

int main(void) {
    int x = 10;
    int y = 20;

    // Estrutura condicional if-else
    if (x > y) {
        printf("x é maior que y\n");
    } else {
        printf("x não é maior que y\n");
    }

    return 0;
}
