#include <stdio.h>

int main(void) {
    int x = 10;          // Declara e inicializa uma variável do tipo inteiro
    float y = 5.5;       // Declara e inicializa uma variável do tipo ponto flutuante
    char c = 'a';        // Declara e inicializa uma variável do tipo caractere
    int arr[5];          // Declara um array de inteiros com 5 elementos
    
    printf("x = %d, y = %.2f, c = %c\n", x, y, c);
    return 0;
}
